export function TituloPrincipal(props) {
    return (
         <header style={{backgroundColor: 'rgba(212, 27, 212, 0.82)', padding: '10px'}}>
           <h1>{props.titulo}</h1>
           </header>
    );
    
}